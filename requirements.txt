# Seed script dependencies (run locally to populate Qdrant Cloud)
# Install: pip install -r requirements.txt
#
# The Cloudflare Worker itself has no requirements.txt —
# its deps (mcp>=1.5.0) are declared in pyproject.toml and
# bundled automatically by wrangler on deploy.

qdrant-client==1.17.1
pydantic==2.12.5
python-frontmatter==1.1.0
PyYAML==6.0.3
python-dotenv==1.2.2
requests==2.32.5

# ── Optional: run the Python FastMCP server locally (for script execution) ────
# pip install -r requirements-server.txt
# then: MCP_TRANSPORT=streamable-http python -m skill_mcp.server
fastmcp==3.2.4
uvicorn==0.45.0

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0
